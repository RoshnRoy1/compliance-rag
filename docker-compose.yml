services:
  backend:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./docs:/app/docs
      - ./chroma_db:/app/chroma_db

  frontend:
    build: ./frontend
    ports:
      - "5173:5173"
    depends_on:
      - backend